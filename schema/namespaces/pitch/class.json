{"pitch_class":
    {
        "value": { 
            "type": "object",
            "properties": {
                "tonic": { "type": "string", "pattern": "^[A-G][b#]*$" },
                "pitch": { "type": "integer" }
            },
            "required": ["tonic", "pitch"]
        },
        "dense": true,
        "descrpition": "Pitch class in (tonic, pitch class) format"
    }
}

